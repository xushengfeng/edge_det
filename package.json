{
  "name": "edge_det",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "build": "pnpm build:wasm && tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "pureimage": "^0.4.18",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
